# AI Chatbot

A terminal chatbot powered by Groq AI and Python.

## Setup
1. Clone this repo
2. Run `pip install groq python-dotenv rich`
3. Create a `.env` file and add:
GROQ_API_KEY=your_key_here
4. Run `python chatbot.py`
